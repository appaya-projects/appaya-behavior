import { SliderBehavior } from './slider.behavior';
describe('Slider behavior', () => {
    it('Slider should be ', () => {
        const a = new SliderBehavior();
        expect(a.content).toBe("Raczki2");
    });
});
//# sourceMappingURL=slider.behavior.spec.js.map